// const submissions = state => state.submissions
const submission = state => state.submission
const submission_reasons = state => state.submission_reasons
const submission_reason = state => state.submission_reason
const submission_type = state => state.submission_type
const submission_bankruptcy_disclosure = state => state.submission_bankruptcy_disclosure

export default {
  submission,
  submission_reasons,
  submission_reason,
  submission_type,
  submission_bankruptcy_disclosure,
};
